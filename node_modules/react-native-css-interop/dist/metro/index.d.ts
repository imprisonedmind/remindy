import type { MetroConfig } from "metro-config";
import { CssToReactNativeRuntimeOptions } from "../types";
export type WithCssInteropOptions = CssToReactNativeRuntimeOptions & {
    input: string;
    platforms?: string[];
    processPROD: (platform: string) => string | Buffer;
    processDEV: (platform: string, next: (update: string) => void) => Promise<string>;
};
export declare function withCssInterop(config: MetroConfig, { platforms, ...options }: WithCssInteropOptions): MetroConfig;
