import { TailwindCliOptions } from "../types";
export declare const tailwindCliV3: {
    processPROD(options: TailwindCliOptions): Buffer;
    processDEV(options: TailwindCliOptions & {
        onChange: (css: string) => void;
    }): Promise<string>;
};
export declare function tailwindConfigV3(path: string): import("tailwindcss/types/config").Config;
